services:
  dind:
    image: docker:27-dind
    privileged: true
    command: dockerd --host=tcp://0.0.0.0:2375
    volumes:
      - dind-data:/var/lib/docker
      - buildkit-cache:/var/lib/buildkit
    networks:
      - opencode

  opencode:
    build: .
    depends_on:
      - dind
    ports:
      - "3000:3000"
    environment:
      - DOCKER_HOST=tcp://dind:2375
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY:?}
      - MOONSHOT_API_KEY=${MOONSHOT_API_KEY:-}
      - GH_TOKEN=${GH_TOKEN}
      - OPENCODE_SERVER_PASSWORD=${OPENCODE_SERVER_PASSWORD}
    healthcheck:
      test: ["CMD-SHELL", "if [ -n \"$OPENCODE_SERVER_PASSWORD\" ]; then curl -fsS -u \"opencode:$OPENCODE_SERVER_PASSWORD\" http://localhost:3000/global/health > /dev/null; else curl -fsS http://localhost:3000/global/health > /dev/null; fi || exit 1"]
      interval: 30s
      timeout: 5s
      start_period: 30s
      retries: 3
    networks:
      - opencode

volumes:
  dind-data:
  buildkit-cache:

networks:
  opencode:
